import { browser } from 'protractor';

describe('Student Form', () => {
  it('should submit the form successfully', () => {
    browser.get('http://localhost:4200/student-form');
    // Perform actions and assertions here
  });
});
